[pytest]
# Limit discovery to the backend and frontend test suites to avoid collecting
# unrelated subprojects that bring heavy optional dependencies.
# This keeps CI and local runs fast and predictable.

# Primary test folders
testpaths = backend/tests

# Allow asyncio marker used across tests
markers =
    asyncio: mark test as asyncio

# Reduce verbosity by default
addopts = -q
